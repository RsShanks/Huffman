void TriSelec(int tab[], int size, int count) {
    int tmp;
    count++;
    for (int i=0; i<size-1; i++) {
        if (tab[i] > tab[i+1]) {
            tmp=tab[i+1];
            tab[i+1]=tab[i];
            tab[i]=tmp;
            TriSelec(tab,size,count);
        }
    }
}
